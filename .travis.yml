language: perl

env:
  - GIT_ROOT_DIR="$TRAVIS_BUILD_DIR/git"

before_install:
  - sudo apt-get update -q
  - sudo apt-get install -y lighttpd php php-cgi php-cli php-curl php-mbstring php-sqlite3 git

install:
  - (cd t && ./install-wiki.sh install)

script:
  - make test GIT_ROOT_DIR=$GIT_ROOT_DIR
